/** Settlement: periodic payout from wallet balance to human bank account. */

// TODO: implement in core-build phase
// - requestSettlement(walletId, amount) → Settlement
// - processSettlement(settlementId) → void (Stripe payout or USDC transfer)
// - getSettlementHistory(projectId) → Settlement[]
