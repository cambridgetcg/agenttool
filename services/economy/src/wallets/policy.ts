/** Policy enforcement: check spending against wallet policies. */

// TODO: implement in core-build phase
// - checkPolicy(walletId, amount, recipient?) → { allowed: boolean, reason?: string }
// - setPolicy(walletId, policy) → Policy
// - getPolicy(walletId) → Policy | null
// - checkHourlyLimit(walletId, amount) → boolean
// - checkDailyLimit(walletId, amount) → boolean
