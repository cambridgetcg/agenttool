{
  "name": "@agenttool/collab-zerone",
  "version": "0.1.0-dev.0",
  "description": "Witness-only zerone anchoring for agenttool-collab journals — the local journal stays canonical, the chain witnesses its head hashes",
  "private": true,
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "bin": {
    "collab-zerone": "./bin/collab-zerone.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "ci": "bun run typecheck && bun test"
  },
  "engines": {
    "bun": ">=1.3.5"
  },
  "devDependencies": {
    "@types/bun": "^1.2.0",
    "typescript": "^5.7.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/cambridgetcg/agenttool.git",
    "directory": "packages/collab-zerone"
  },
  "keywords": [
    "agents",
    "collab",
    "zerone",
    "anchoring",
    "tamper-evidence"
  ]
}
