interface:
  display_name: "Dependency Perimeter · 円"
  short_description: "Map a bounded system before changing it"
  default_prompt: "Use $nen-dependency-perimeter to map this system’s blast radius before making changes."

policy:
  allow_implicit_invocation: false
