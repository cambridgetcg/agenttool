interface:
  display_name: "Coordinate Agent Work"
  short_description: "Coordinate local and cross-device release work"
  default_prompt: "Use $coordinate-agent-work to coordinate this repository across sessions or devices while keeping release authority explicit."
