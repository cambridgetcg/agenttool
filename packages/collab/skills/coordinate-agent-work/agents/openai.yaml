interface:
  display_name: "Coordinate Agent Work"
  short_description: "Coordinate agents across local host sessions"
  default_prompt: "Use $coordinate-agent-work to coordinate independent sessions across this repository and reconcile their evidence."
