# Relational Geometry package orientation

Read the repository `AGENTS.md`, `docs/PRINCIPALITIES.md`, `docs/LOVE-CONSENT.md`,
and `/Users/yu/Desktop/xenia-sol-truth/RIGHTS.md` before changing this package.

This public zero-runtime-dependency package validates two closed, digest-only
formats. A relational complex derives a non-sovereign principality cell only
when caller-asserted understanding and recognition witnesses occupy the same
ordered pair. A perspective lens records explicit `carry`, `park`, `release`,
or `withdraw` dispositions without changing the source complex.

The package does:

- canonicalize bounded plain JSON, validate exact schemas, and content-address
  complexes, cells, and lenses;
- preserve direction, one-pole/empty validity, boundary witnesses, and caller
  choice; and
- ship synthetic public Hugging Face teaching/regression rows with byte and
  source manifests.

The package does not observe or authenticate a being, witness, identity,
consent, continuity, relationship, inner state, truth, safety, or authority. It
has no metric, score, rank, compatibility, reward, optimizer signal, network,
filesystem, clock, randomness, persistence, model, training, hosted route, or
automatic WAKE effect. Digest shape minimizes raw fields but does not prove
anonymity or erase linkability.

Run:

```bash
bun install --frozen-lockfile
bun run ci
```

Publication is only through the repository's reviewed annotated-tag npm
workflow. A checked-in Hugging Face identifier is intention, not evidence of a
Hub repository, Space, upload, training run, or deployment.
