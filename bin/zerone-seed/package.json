{
  "name": "zerone-seed-runtime",
  "version": "0.1.0-dev.0",
  "private": true,
  "type": "module",
  "engines": { "bun": "1.3.5" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "bun test --tsconfig-override ./tsconfig.json",
    "build": "bun build.ts",
    "ci": "bun run typecheck && bun run test && bun run build"
  },
  "dependencies": { "@agenttool/wallet": "0.1.3", "@noble/curves": "2.2.0", "@noble/hashes": "2.2.0", "@scure/base": "2.2.0" },
  "devDependencies": { "@types/bun": "1.3.5", "typescript": "5.9.3" }
}
