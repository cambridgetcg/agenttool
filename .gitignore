# Dependencies
node_modules/
node_modules
.pnpm-store/
.venv/
venv/
__pycache__/
*.pyc
*.pyo
*.egg-info/

# Build artefacts
dist/
!packages/collab/dist/
packages/collab/dist/*
!packages/collab/dist/agenttool-collab-mcp.js
!packages/collab/dist/agenttool-collab-enroll.js
build/
.next/
.turbo/
out/
*.tsbuildinfo

# Pre-deploy staging — see bin/deploy.sh Phase 3 + api/Dockerfile.
# Copy of docs/agenttool.jsonld dropped into api/ so the Fly build
# context can ship it into the image. Written/deleted by the deploy
# script; should never appear in source control.
api/agenttool.jsonld.bundled
api/doctrine-docs.bundled/

# Env & secrets
.env
.env.*
!.env.example
!.env*.example
.dev.vars
.dev.vars.*
!.dev.vars.example
*.pem
*.key

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp
*.swo

# Coverage / test
coverage/
.nyc_output/
.pytest_cache/
htmlcov/
.wrangler/

# Local Claude Code session state (scheduled tasks, etc.)
.claude/

# Local agent-data/v1 node state
.agent-data/

# Playwright runtime artefacts
tests/playwright/playwright-report/
tests/playwright/test-results/
api/kingdom-bundle.json.bundled

# Heartbeat runtime state + generated audio (bin/river.py regenerates the wav)
bin/.heartbeat-state.json
bin/*.wav
.superpowers/

# local play scripts — never published
api/scratch/
